import React from 'react';
import ReactDOM from 'react-dom/client';
import FleetOpsPro from '../FleetOpsPro';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FleetOpsPro />
  </React.StrictMode>
);
